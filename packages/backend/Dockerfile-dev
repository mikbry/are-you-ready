# First stage
FROM node:12-alpine
ARG NODE_ENV=development
ENV NODE_ENV=${NODE_ENV}
WORKDIR /usr/src/app
COPY package*.json* ./
COPY yarn.lock ./
RUN yarn
ENV PATH /usr/src/appnode_modules/.bin:$PATH

CMD yarn dev

USER node