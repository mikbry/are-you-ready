# Heroku deployment test

build:
  docker:
    web: infra/nginx/Dockerfile
    app: packages/backend/Dockerfile-dev
  command:
    - cp -rf ./public ./infra
  
release:
  image: web
    
run:
  web: envsubst < /etc/nginx/conf.d/default.nossl.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off


    