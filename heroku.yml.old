# Heroku deployment test

build:
  config:
    APP_NAME: AYR
    APP_BACKEND_HOST: localhost
    APP_BACKEND_PORT: 3032
    WEB_TEMPLATE: default.nossl.template
    WEB_CONF: nginx.min.conf
    COMPOSE_PROJECT_NAME: Areyouready
    IMAGE_VERSION: latest
  docker:
    web: www/Dockerfile-heroku
    api: packages/backend/Dockerfile
  
    